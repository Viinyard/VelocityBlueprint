<?xml version="1.0" encoding="UTF-8" ?>
<model xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="monModel" xsi:noNamespaceSchemaLocation="model.xsd">
    <!--
    A property is a value that will be used in the model. It can be used in the directives and in the prompts.
    Properties are optional.
    <properties>
        <property key="KEY">The SRC property is ${SRC}</property>
        <property key="KEY2"/>
        <property key="KEY3"/>
        <property key="KEY4"/>
    </properties>

    A directive is a file that will be generated by the generator.
    Multiple directives can be defined in the same model. It must contain at least one directive.
    <directives>
        <directive name="Exemple of a model directive" template="exampleTemplate.vm">
            ${SRC}/${PACKAGE}/core/Example.txt
        </directive>
    </directives>

    Each value of a prompt must have a unique key defined in the properties section (It may be left empty).
    If you want to use a value from the prompt in the next prompt, you can use the ${KEY} syntax, pay attention to order of the prompts to do so.
    <prompts>
        <monoSelect value="KEY2" order="1">
            Must contain at least one value
            <value key="choice 1" selected="true" enabled="false">${KEY}</value>
            <value key="choice 2" selected="false" enabled="true">Example</value>
        </monoSelect>
        <multiSelect value="KEY3" order="2">
            Must contain at least one value
            <value key="choice 1" selected="true" enabled="false">${KEY2}</value>
            <value key="choice 2" selected="false" enabled="true">valeur</value>
        </multiSelect>
        <stringInput value="KEY4" masked="false" order="3">
            // The default value is optional
            <defaultValue>${SRC}</defaultValue>
        </stringInput>
    </prompts>
    -->

    <directives>
        <directive name="" template=""></directive>
    </directives>
</model>
